# Example Profile - Induction Heater Setup
# When loaded, missing values will use defaults from shared_state

temperature_setpoint=190

# Heater coil resistance in ohms
heater_resistance=0.48

# Maximum watts limit for this heater
max_watts=85

# PID tuning for this specific heater setup (P,I,D)
pid_temperature_tunings = 2.3,0.03,0

# Degrees above setpoint at which PID will be reset if temperature spikes after reaching setpoint
pid_reset_high_temperature=8

# Enable autosession data logging to file
autosession_logging_enabled=false

# Default autosession profile to load (leave empty or omit to use current_autosession_profile.txt)
# default_autosession_profile=

# Autosession log buffer flush threshold (number of lines before flushing to file)
# autosession_log_buffer_flush_threshold=20

#hardware=broken_thermocouple_test
